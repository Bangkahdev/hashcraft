"""Local digest algorithms, digest validation, and verification (PRD 7).

Everything here operates entirely locally: no network access,
telemetry, credential collection, or online authentication attempts,
per PRD 3's "operate entirely locally" constraint.
"""
